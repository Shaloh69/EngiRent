# Core
python-dotenv==1.0.1
structlog==24.4.0

# Socket.io client — pulled in by flask-socketio; aiohttp activates async client support
aiohttp>=3.9.0

# GPIO (installed system-wide via apt — imported here for venv awareness)
# gpiozero and lgpio are provided by python3-gpiozero / python3-lgpio (apt)
# venv must be created with --system-site-packages

# Camera
# picamera2 and opencv are provided by python3-picamera2 / python3-opencv (apt)
# do NOT pip-install these — they conflict with system packages on Pi OS Trixie

# Image upload to Supabase
supabase==2.15.3
pillow==11.0.0

# Local kiosk UI server
flask==3.1.0
flask-socketio==5.4.1

# WiFi provisioning
flask-cors==5.0.0
