# Core
python-dotenv==1.0.1
structlog==24.4.0

# Socket.io client — pulled in by flask-socketio; aiohttp activates async client support
aiohttp>=3.9.0

# GPIO (installed system-wide via apt — imported here for venv awareness)
# lgpio is provided by python3-lgpio (apt). gpiozero is deliberately NOT
# installed — the hardware layer (hardware/gpio_controller.py,
# hardware/actuator_controller.py) migrated to direct lgpio calls; gpiozero's
# own pin-factory daemon (lgd) actively conflicts with this (it pre-claims
# GPIO pins), which is why setup.sh explicitly stops/disables it.
# venv must be created with --system-site-packages

# Camera
# opencv is provided by python3-opencv (apt) — do NOT pip-install it, it
# conflicts with the system package on Pi OS Trixie. picamera2 is not used
# at all — all 5 cameras are USB (via OpenCV/GStreamer), not a Pi Camera
# Module.

# Image upload — POSTs to the Node backend (see services/image_uploader.py),
# not a direct storage SDK
pillow==11.0.0

# Local kiosk UI server
flask==3.1.0
flask-socketio==5.4.1

# WiFi provisioning
flask-cors==5.0.0
